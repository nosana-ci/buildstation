nosana:
  description: Nosana-Node Pipeline

global:
 # docker pull 
  image: registry.hub.docker.com/library/solanalabs/solana:stable

  trigger:
    branch:
      - master

jobs:
  - name: Hello World Solana
    commands:
      - ls
      - npm install
      - npm run build:program-rust
      