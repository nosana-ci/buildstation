nosana:
  description: Nosana-Node Pipeline

global:
  image: registry.hub.docker.com/library/solana:stable

  trigger:
    branch:
      - master

jobs:
  - name: Hello World Solana
    commands:
      - ls
      - npm install
      - npm run build:program-rust
      